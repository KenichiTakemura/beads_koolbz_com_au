<div class="container">
	<div class="row shopping-cart" >
		<div class="row-fluid">
		<span class="label label-info"><i class="icon-shopping-cart icon-white"></i><%=t("cart.mycart")%></span>
			<span class="label" id="mycart_total_price">
			<% if @mycart.present? && @mycart.item.present? %>
				<%=show_total_price()%>
			<% else %>
				<%= t("cart.noitem")%>
			<% end %>
			</span>
			<%=link_to_remote(t("op.clear_mycart"),"itemcases/clear_mycart","btn btn-primary btn-link","")%>

		</div>
		<div class="row-fluid">
			<div id="mycart">
			<%=show_cart(false)%>
			</div>
		</div>
		<div class="row-fluid">
			<span id="item-checkout" class="">
				<%=show_checkout()%>
			</span>
		</div>
	</div>
	<% if @items.present? %>
	<div class="row">
		<div class="">
			<!-- Carousel
			================================================== -->
			<div id="itemCarousel" class="carousel itemcase slide">
				<div class="carousel-inner">
					<% @items.each_with_index do |item,i| %>
					<% token = Common.unique_token %>
					<div class="item" id="<%="#{token}_#{item.id}"%>">
						<ul class="thumbnails" >
							<%= show_itemcase(@items,i)%>
						</ul>
					</div>
					<% end %>
				</div>
				<a class="left carousel-control" href="#itemCarousel" data-slide="prev">&lsaquo;</a>
				<a class="right carousel-control" href="#itemCarousel" data-slide="next">&rsaquo;</a>
			</div><!-- /.carousel -->
		</div>
	</div>
	<div class="row">
		<div class="container">
			<div class="span6" id="items_desc">
				<%= show_itemdesc(@items.first)%>
			</div>
		</div>
	</div>
	<% else %>
	<div class="row-fluid">
		<p class="wall muted"><%= t("item.noiteminthesop") %></p>
	</div>
	<% end %>
</div>
