<!-- NAVBAR
================================================== -->
<!-- Wrap the .navbar in .container to center it on the page and provide easy way to target it with .navbar-wrapper. -->
<div class="container navbar-wrapper">

	<div class="navbar navbar-inverse">
		<div class="navbar-inner">
			<!-- Responsive Navbar Part 1: Button for triggering responsive navbar (not covered in tutorial). Include responsive CSS to utilize. -->
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </a>
			<a class="brand" href="<%=root_path(:locale => "en")%>"><%=show_logo%></a>
			<div class="nav-collapse collapse">
				<ul class="nav">
					<li class="active dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t("beads.selection")%><b class="caret"></b></a>
						<ul class="dropdown-menu">
							<li class="nav-header">
								<%= t("beads.regular")%>
							</li>
							<% @regular.each do |category| %>
							<li>
								<a href="<%=Enclink.link_to(category.name)%>"><%= category.name %></a>
							</li>
							<% end %>
							<li class="divider"></li>
							<li class="nav-header">
								<%= t("beads.special")%>
							</li>
							<% @special.each do |category| %>
							<li>
								<a href="<%=Enclink.link_to(category.name)%>"><%= category.name %></a>
							</li>
							<% end %>
						</ul>
					</li>
				</ul>
			</div><!--/.nav-collapse -->
			<%= render :partial => "layouts/signedin" %>
		</div><!-- /.navbar-inner -->
	</div><!-- /.navbar -->

</div><!-- /.container -->

