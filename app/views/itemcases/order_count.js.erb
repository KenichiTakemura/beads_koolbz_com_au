$("#item_total_price_desktop_<%=@item.id%>").html("<%=escape_javascript(number_to_currency(@item.total_price(@count), :locale => "au"))%>");
$("#item_total_price_phone_<%=@item.id%>").html("<%=t("item.subtotal_price")%> <%=escape_javascript(number_to_currency(@item.total_price(@count), :locale => "au"))%>");
$("#order_total_price").html("<%=escape_javascript(show_order_total_price)%>");